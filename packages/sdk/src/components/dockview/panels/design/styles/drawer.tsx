import type { ThemePreset } from "@/lib/theme-presets";

export function StylesDrawer(_props: {
  activeId: string | null;
  onPick: (preset: ThemePreset) => void;
  onClear: () => void;
}) {
  return null;
}
